#pragma warning(disable :4996)
#include<bits/stdc++.h>
using namespace std;
int dp[1001], A_len, B_len;
char A[1010], B[1010];
int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    cin >> A >> B;
    A_len = (int)strlen(A), B_len = (int)strlen(B);
    for (int i = 1; i <= A_len; i++) {
        for (int j = 1; j <= B_len; j++) {

        }
    }
    return 0;
}